---
const stack = [
  {
    category: "Frontend Architecture",
    items: ["React", "Vue", "Next.js", "Astro", "TypeScript"]
  },
  {
    category: "Backend & Core",
    items: ["Node.js", "Go", "Python", "Laravel", "PostgreSQL"]
  },
  {
    category: "Cloud & Infrastructure",
    items: ["AWS", "Docker", "Kubernetes", "Terraform", "CI/CD Pipelines"]
  }
];
---
<section id="tech-stack" class="tech-stack">
  <div class="container">
    <span class="section-subtitle">Our Arsenal</span>
    <h2 class="section-title">Production-Grade <span class="text-gold">Tech Stack</span></h2>
    
    <div class="stack-grid">
      {stack.map(group => (
        <div class="stack-category">
          <h3 class="category-title">{group.category}</h3>
          <ul class="stack-list">
            {group.items.map(item => (
              <li class="stack-item">
                <span class="bullet"></span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .tech-stack {
  padding: var(--section-padding);
  background-color: rgba(60, 40, 30, 0.7); /* opacity 0.85 */
  color: var(--color-white);
  }

  .tech-stack .section-title {
    color: var(--color-white);
  }

  .stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 4rem;
    margin-top: 5rem;
  }

  .category-title {
    font-size: 1.25rem;
    color: var(--color-gold);
    margin-bottom: 2rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-bottom: 1px solid rgba(212, 175, 55, 0.2);
    padding-bottom: 1rem;
  }

  .stack-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .stack-item {
    font-size: 1.1rem;
    display: flex;
    align-items: center;
    color: rgba(255, 255, 255, 0.85);
    transition: color 0.3s ease;
  }

  .stack-item:hover {
    color: var(--color-gold-light);
  }

  .bullet {
    width: 8px;
    height: 8px;
    background-color: var(--color-gold);
    border-radius: 50%;
    margin-right: 1rem;
    display: inline-block;
  }

  .text-gold {
    color: var(--color-gold);
  }
</style>
